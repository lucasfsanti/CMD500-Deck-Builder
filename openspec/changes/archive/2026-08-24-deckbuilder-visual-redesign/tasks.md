## 1. Token system

- [x] 1.1 Replace the `:host, :root` custom properties in `extension/src/ui/panel.css` with the "Ledger, Reforged" token set from design.md (`--c500-ink`, `--c500-ink-soft`, `--c500-paper`, `--c500-paper-raised`, `--c500-line`, and the seven mana tokens `--c500-mana-{w,u,b,r,g,gold,c}`, with `-r`/`-g`/`-gold` aliased to the existing rust/forest/brass values) and verify `npm run build` (or the project's existing build script in `extension/package.json`) compiles with no unresolved custom-property references.
- [x] 1.2 Delete the dead overlay-only rules from `panel.css`: `.c500-panel` container, and the `.c500-gauge`/`.c500-legality`/`.c500-export` variants that only the removed floating panel used (keep any variant still referenced by the tab view under its new name from task 1.3) and verify `grep -rn "c500-panel\b\|c500-gauge\b\|c500-legality\b\|c500-export\b" extension/src` shows no remaining references to the deleted selectors outside `panel.css` itself.
- [x] 1.3 Rename `c500-panel__mark` → `c500-tab__mark` and `c500-panel__format` → `c500-tab__format-select` in both `panel.css` and `extension/src/tab/TabRoot.tsx`, and verify `TabRoot.test.tsx` still passes.

## 2. Ledger tape

- [x] 2.1 Add a `.c500-tab__ledger-tape` full-width horizontal-strip layout in `panel.css` (flex row, compact inline sizing for its children) and mount `BudgetGauge`, `CardCountGauge`, `LegalitySummary`, and `ExportMenu` inside it in `TabRoot.tsx`, replacing their current stacked position in `.c500-tab__sidebar`.
- [x] 2.2 Restyle `BudgetGauge`/`CardCountGauge` (`extension/src/ui/components/BudgetGauge.tsx`, `CardCountGauge.tsx`) and their CSS for the compact inline form the ledger tape needs (smaller gauge or a non-gauge compact stat treatment — keep each component's props/computation untouched) and verify `BudgetGauge.test.tsx` and `CardCountGauge.test.tsx` pass unchanged.
- [x] 2.3 Move the view-mode toggle (Lista/Visual) and grouping-axis select out of dedicated sidebar blocks into a slim control row adjacent to the ledger tape in `TabRoot.tsx`, and verify existing `TabRoot`-level tests that exercise view/grouping switching still pass.

## 3. Color-identity rail

- [x] 3.1 Add a small pure function (co-located with `card-art.ts` or a new `color-identity.ts` under `extension/src/ui/components/`) mapping a `CardEnrichment["colorIdentity"] | undefined` to a mana CSS token name: single entry → that color, `length > 1` → gold, `length === 0` → colorless gray, `undefined` (enrichment pending) → neutral line color. Add a unit test covering all four cases.
- [x] 3.2 Apply the mapped token as a left-edge rail (2–4px `border-left` or a pseudo-element) on `.c500-card` (`CardRow.tsx`) and `.c500-tile` (`CardVisualTile.tsx`) in `panel.css`, and verify `CardRow.test.tsx`/`CardVisualTile.test.tsx` (extended with a case per component asserting the rail color reflects `colorIdentity`) pass.
- [x] 3.3 Add the same keyline treatment described in design.md for the white-mana token specifically (thin `--c500-ink`-toned inner stroke) and manually verify contrast against `--c500-paper` and `--c500-paper-raised`.

## 4. Analytics recoloring

- [x] 4.1 Extend `Bucket` (`extension/src/lib/analytics/bucket-counts.ts`) or `BarChart`'s props (`extension/src/ui/components/charts/BarChart.tsx`) so each bucket can carry an optional color token, and have `colorBuckets()` populate it using the `White/Blue/Black/Red/Green/Multicolor/Colorless` label already produced today, mapped 1:1 to the seven mana tokens; leave `manaCurveBuckets()`/`typeBuckets()` producing no color (chart renders its existing single neutral hue). Verify `bucket-counts.test.ts` covers the new color field for `colorBuckets()`.
- [x] 4.2 Update `BarChart.tsx` to use a bucket's color token for `.c500-chart__bar` when present, falling back to the current neutral hue otherwise, and verify `BarChart.test.tsx` (extended with a colored-bucket case) passes.
- [x] 4.3 Lay out the three `BarChart` instances (Curva, Cor, Tipo) horizontally as the "analytics band" in `TabRoot.tsx` per design.md's layout, alongside the Commander hero block from Task 5, and verify the layout doesn't break at the `900px`/`700px` breakpoints already defined in `panel.css`.

## 5. Commander hero and layout restructure

- [x] 5.1 Add a hero-sized variant of `CardVisualTile` (larger `.c500-tile__art` dimensions via a class modifier, reusing `resolveCardArt` and the existing placeholder path unchanged) and verify a new test case confirms the placeholder still renders when artwork is unresolved, matching `card-visual-view`'s existing spec behavior.
- [x] 5.2 Render the `comandante`/`comandanteParceiro` zones in the new Commander hero block in `TabRoot.tsx` using the hero tile variant, removing them from the narrow `.c500-tab__zones-side` column; keep `ZoneSection`'s existing props/logic (drag-and-drop, grouping, error display) intact — only the container they're rendered into changes.
- [x] 5.3 Restructure `TabRoot.tsx`'s top-level layout to: header → ledger tape + control row (Task 2) → Commander hero + analytics band (Task 5.2/4.3) → Main Deck (dominant) + Maybeboard side column, updating `.c500-tab__body`/`.c500-tab__zones` grid rules in `panel.css` accordingly, and verify the drag-and-drop `DndContext` still resolves drops correctly between all four zones (existing `resolve-drop`/`TabRoot` tests pass unchanged, since zone ids and `useDroppable`/`useDraggable` wiring are untouched).

## 6. Verification pass

- [x] 6.1 Run the full test suite (`npm test` in `extension/`) and confirm all tests pass, updating only tests that assert on now-changed DOM/CSS-class shape (per design.md's risk note — component logic/props tests should need no changes).
- [x] 6.2 Manually walk every scenario in `card-visual-view`, `deck-organizer`, `deck-analytics`, `budget-tracking`, and `deck-size-limit`'s specs against the redesigned tab view (view toggle, drag-and-drop, illegal/over-budget markers, quantity editing, removal, all three chart types, over-cap states) and confirm each still holds with the new visuals.
- [x] 6.3 Load the unpacked extension, open a real LigaMagic deck's full-tab view, and visually confirm: the ledger tape reads as one coherent strip, the color-identity rail is visible but not noisy across a full Main Deck list, the white-mana rail is legible against the parchment background, and the Commander hero block shows real artwork (or the placeholder) at the new larger size.

## 7. Live-review follow-ups

- [x] 7.1 Restructure the main content row: a left column holds the Commander hero (Comandante and Comandante Parceiro rendered side by side, widened enough to fit both at hero size) with Maybeboard's normal (non-hero) zone below it, sitting beside Deck Principal (dominant width) at the same vertical start — so Deck Principal's header is visible in the initial viewport without scrolling. Move the Curva/Cor/Tipo analytics band to a full-width row below this main content row. Verify Deck Principal's zone header appears above the fold at a typical desktop window size, and that drag-and-drop still resolves correctly between all four zones (existing `resolve-drop`/`TabRoot` tests pass unchanged — zone ids and `useDroppable`/`useDraggable` wiring untouched).
- [x] 7.2 Replace the Lista/Visual toggle's text-labeled buttons with icon-only buttons (list-rows icon for Lista, grid/tiles icon for Visual), adding an `aria-label` to each (e.g., "Ver em lista" / "Ver em modo visual") since the icon removes the visible text that previously served as the accessible name. Verify each button is still queryable by its new accessible name and `aria-pressed` state, and that existing `TabRoot` tests exercising the toggle still pass (updated to query by the new aria-label if they queried by visible text).
- [x] 7.3 Increase the default (non-hero) `.c500-tile__art` dimensions — used by Visual-mode grid tiles and, since `CardRow`'s hover preview reuses the same tile art classes, the List-mode hover preview too — keeping the 5:7 card aspect ratio. Verify `.c500-tile-grid`'s flex-wrap layout and `.c500-hover-preview`'s fixed positioning still render correctly at the new size, and that `CardVisualTile.test.tsx`/`CardRow.test.tsx` still pass.

## 8. Second live-review follow-ups

- [x] 8.1 Before starting on this group's spec-touching tasks (8.8), run `/opsx:continue` to create `specs/deck-organizer/spec.md` from the drafted delta content in this update's review (Comandante removal-control exception) — required so the implementation in 8.8 has a written spec to match, not just a proposal-doc note.
- [x] 8.2 Remove the visible name caption from non-hero `CardVisualTile` renders (Deck Principal/Maybeboard grid tiles), keeping it on hero renders (`size="hero"`) and keeping the `alt` text on the `<img>` unchanged either way. Verify `CardVisualTile.test.tsx` covers both: no visible caption text at default size, caption still present at hero size.
- [x] 8.3 Bump the base type scale slightly across `panel.css` (body text, labels, zone headers, ledger amounts) and add a dedicated `--c500-price` token (darker than `--c500-mana-gold`, targeting ≥4.5:1 against both `--c500-paper` and `--c500-paper-raised`) used only by `.c500-card__price`/`.c500-tile__price`; leave every other use of `--c500-mana-gold` (rail, dots, marks) unchanged. Verify with a contrast computation like the one used for `--c500-mana-w` in task 3.3.
- [x] 8.4 Rename the `mainDeck` zone's display label from "Deck Principal" to "Main Deck" and the `comandanteParceiro` zone's from "Comandante Parceiro" to "Companheiro" in `ZoneSection.tsx`'s `ZONE_LABELS`. Update `TabRoot.test.tsx`/`ZoneSection.test.tsx` assertions that reference the old label text.
- [x] 8.5 Move the analytics band into a third grid column to the right of Deck Principal (stacking the three `BarChart`s vertically within that column instead of side by side), and reduce `.c500-tab__body`'s side padding / increase its `max-width` so the three columns (hero column, Deck Principal, analytics) fit comfortably. Verify the layout doesn't break at existing breakpoints, adjusting them if needed for the third column.
- [x] 8.6 Move Companheiro (`comandanteParceiro`) back to stacking below Comandante instead of beside it (revert the `flex: 1 1 140px` side-by-side sizing from task 7.1 for this pair), and add a shrink-when-empty state: when its `cards` array is empty, the hero dropzone collapses to a small "drop a partner here" hint instead of reserving full hero-art height; it expands back to full height once a card is present. Verify a `ZoneSection` test asserting the empty vs. non-empty height/class difference.
- [x] 8.7 Give Deck Principal's List view a multi-column grouped layout (CSS `column-width` on the dropzone, `break-inside: avoid-column` on each group's wrapper), similar in shape to the Moxfield reference; scope this to Deck Principal only — Maybeboard's `ZoneSection` instance keeps its existing single-column stacked list. Verify visually that Deck Principal's groups flow into columns at a typical desktop width and that Maybeboard is unaffected.
- [x] 8.8 In `TabRoot.tsx`, omit the `onRemoveCard` prop when rendering the `comandante` `ZoneSection` (its `CardVisualTile` already renders no remove control when `onRemove` is undefined — no new prop needed elsewhere). Verify a test asserting no removal control renders for the Comandante card while Companheiro/Maybeboard/Deck Principal are unaffected, and that this matches the `deck-organizer` delta spec created in 8.1.
- [x] 8.9 Increase `.c500-card__qty`/`.c500-tile__qty`'s width, height, and font-size enough that a two-digit quantity (e.g., a 20-Island stack) is fully visible, and fix whatever is currently clipping/hiding the digits. Verify visually and via `CardRow.test.tsx`/`CardVisualTile.test.tsx`'s existing quantity-field assertions.
- [x] 8.10 A further pass of enlarging non-hero card sizing for readability — both `CardRow`'s row height/name/price font-size and `.c500-tile__art`'s default (non-hero) dimensions from task 7.3 — beyond the previous round's bump. Verify visually against a real deck and that the full test suite still passes.

## 9. Second verification pass

- [x] 9.1 Run the full test suite (`npm test` in `extension/`) and `tsc --noEmit`, confirming everything passes.
- [x] 9.2 Load the unpacked extension, open a real LigaMagic deck's full-tab view, and visually confirm every item in this update: no name captions on Deck Principal/Maybeboard grid tiles (hero still has one), visibly bigger fonts, higher-contrast price text, "Main Deck"/"Companheiro" labels, analytics as a third column with stacked charts, Companheiro stacked below Comandante and shrinking to a hint when empty, Deck Principal's List view in multiple columns, larger non-hero cards, no remove control on the Comandante's card, and a legible basic-land quantity input.

## 10. Night Ledger palette pivot

- [x] 10.1 In `panel.css`'s token block, rename `--c500-ink`/`-ink-soft`/`-paper`/`-paper-raised` to `--c500-text`/`-text-soft`/`-bg`/`-bg-raised` with the new dark values (`#e8e6e3`/`#8f8d89`/`#17171b`/`#1f1f24`); retune the seven mana tokens to `-g #6b9080`, `-r #c17662`, `-gold #c9a463`, `-w #cfc6b8`, `-u #7897b3`, `-b #9b87a8`, `-c #9a958a`; retune `--c500-price` to `#d1a866`; set `color-scheme: dark`; and change `--c500-status-good-soft`/`-bad-soft` to the rgba overlays `rgba(107, 144, 128, 0.16)`/`rgba(193, 118, 98, 0.16)`. Update every `var(--c500-ink*)`/`var(--c500-paper*)` reference across `panel.css` to the renamed tokens. Verify `npm run build` compiles with no unresolved custom-property references.
- [x] 10.2 In `mana-colors.ts`, move the `keyline: true` flag from the White branch to the Black branch of `manaRailForColorIdentity` (white no longer needs it against the dark base; black now does), and update `CardRow.tsx`/`CardVisualTile.tsx`'s inline keyline stroke color from `--c500-ink` to `--c500-text` (a light stroke on a dark base, mirroring the old dark-on-light stroke). Verify `mana-colors.test.ts`'s keyline assertion moves from White to Black, and manually verify contrast against `--c500-bg`/`--c500-bg-raised`.
- [x] 10.3 Remove `--c500-font-display` and its three usages (`.c500-zone__header`, `.c500-chart__title`, `.c500-tab__title`) in favor of `--c500-font-body`. Verify no remaining `var(--c500-font-display)` references in `panel.css`.
- [x] 10.4 Remove the `.c500-tab__ledger-tape > *:not(:first-child)` divider-line rule; widen the tape's gap slightly to compensate for the lost visual separation. Verify visually.
- [x] 10.5 Flip `.c500-tile__qty`/`.c500-tile__remove`'s overlay chip background from the light `rgba(247, 240, 222, ...)` to a dark `rgba(23, 23, 27, ...)`. Verify visually against real card artwork.
- [x] 10.6 Run the full test suite, `tsc --noEmit`, and `npm run build`; fix any test still asserting the old CSS variable names or the White-keyline behavior task 10.2 moved.
- [x] 10.7 Load the unpacked extension, open a real LigaMagic deck's full-tab view, and visually confirm: the dark near-black canvas, muted mana/status colors legible throughout (rail, ledger tape, charts, error/over-budget states), the black-mana rail legible via its new light keyline, no leftover serif headers anywhere, no ledger-tape divider lines, and native form controls (format/grouping selects, quantity input) rendering with dark chrome.

## 11. Sticky toolbar and analytics

- [x] 11.1 Add `position: sticky; top: 0;` plus a `z-index` to `.c500-tab__ledger-tape` in `panel.css`. Verify it stays pinned to the top of the viewport as the page scrolls, with card rows/tiles scrolling up underneath it without showing through (its background is already opaque).
- [x] 11.2 Add `position: sticky;` with a `top` offset that clears the ledger tape's height, plus `align-self: start`, to `.c500-tab__analytics` — scoped to the `>=1200px` breakpoint where it's a real third grid column (the existing `@media (max-width: 1200px)` rule already drops it to a full-width block, where sticky should not apply). Verify it stays visible beside Deck Principal as that column scrolls, without overlapping the sticky ledger tape above it, and that it reverts to normal (non-sticky) flow below 1200px.
- [x] 11.3 Run the full test suite, `tsc --noEmit`, and `npm run build` (no test changes expected — this is CSS-only, no markup or class-name changes), then load the unpacked extension and visually confirm, scrolling a real deck's full-tab view: the ledger tape and analytics column both stay visible throughout the scroll, neither overlaps the other or any zone content, and the hero column (Comandante/Companheiro/Maybeboard) scrolls away normally.

## 12. Sort-by axis, budget display, group counts, and chart land handling

- [x] 12.1 Before starting on 12.2 (whose implementation must match a written spec), run `/opsx:continue` to create `specs/deck-analytics/spec.md` from the drafted delta content in this update's review (mana curve excludes lands; color chart gains a Terrenos bucket) — this capability has no delta file in this change yet.
- [x] 12.2 In `extension/src/lib/organizer/group-sort.ts`, add a `SortAxis` type (`"cmc" | "name" | "color" | "price"`) and replace `groupByType`/`groupByColor`/`groupByCmc`'s hardcoded multi-axis `.sort()` comparators with one comparator driven by the active sort axis (mana value ascending, name alphabetical, color in the existing colorless/W/U/B/R/G/multicolor order, price descending with unresolved-price cards sorted after all priced cards), falling back to name as the tiebreak in every case. Update `groupAndSortZone`'s signature to accept the sort axis alongside the grouping axis. Verify `group-sort.test.ts` covers all four sort axes crossed with at least the default (Type) grouping axis, matching the new `deck-organizer` delta's scenarios.
- [x] 12.3 In `TabRoot.tsx`, add `sortAxis` state (default `"cmc"`) and a new "Ordenar por" `<select>` next to the existing grouping select in the ledger tape, with the four axis options; pass `sortAxis` through to every zone's `ZoneSection` call alongside the existing `groupingAxis` prop, and thread it into `ZoneSection`'s call to `groupAndSortZone`. Verify a `TabRoot`/`ZoneSection` test exercises switching the sort axis and confirms card order changes accordingly (e.g., switching to Price re-orders a fixture zone by descending price).
- [x] 12.4 In `BudgetGauge.tsx`, change the displayed text from "amount spent / of R$500 limite" to both the amount used and the amount remaining (e.g., "R$414,27 usados · R$85,73 restantes"), computed from the existing `BudgetResult` (`total`, and `BUDGET_CAP - total` floored at 0 for the remaining figure) — no change to the over-cap/incomplete visual states, which stay driven by the same `isOverCap`/`cardsMissingPrice` fields as today. Verify `BudgetGauge.test.tsx`'s existing assertions are updated for the new text and still cover the under-cap, over-cap, and incomplete-total cases.
- [x] 12.5 In `ZoneSection.tsx`, add a card count to `.c500-group__label` (computed from `group.cards` the same way the zone header's count is computed from `cards`) and bump `.c500-group__label`'s font-size and color contrast in `panel.css` (moving off `--c500-text-soft` toward `--c500-text` or a comparably higher-contrast tone). Verify a `ZoneSection` test asserts the group label's rendered text includes the count (e.g., "Criatura (30)" or equivalent).
- [x] 12.6 In `extension/src/lib/analytics/bucket-counts.ts`, filter land cards (via `primaryType(card.enrichment?.typeLine) === "Land"`) out of `manaCurveBuckets()` entirely, and in `colorBuckets()` route land cards into a new `"Terrenos"` bucket (using the new `--c500-mana-land` token from panel.css) instead of their normal color-identity bucket, ordered last (after Multicolor). Verify `bucket-counts.test.ts` covers: a land card excluded from `manaCurveBuckets()`'s output, and a land card (including one with a non-empty color identity, e.g. a dual land) landing in the Terrenos bucket rather than a color bucket in `colorBuckets()`'s output — matching the new `deck-analytics` delta's scenarios from 12.1.
- [x] 12.7 Add `--c500-mana-land: #a68a6b` to `panel.css`'s token block, alongside the other mana tokens, with a short comment noting why it's distinct from `--c500-mana-c` (colorless) despite lands often having no color identity of their own.
- [x] 12.8 Run the full test suite, `tsc --noEmit`, and `npm run build`, then load the unpacked extension and visually confirm against a real deck: the "Ordenar por" select re-orders cards within each zone/group as expected for all four axes (including Price, highest-first, with unresolved-price cards trailing), the budget meter shows both used and remaining amounts, group labels show a card count at a visibly larger/higher-contrast size, the mana curve chart has no land-driven spike at CMC 0, and the color chart shows a distinct "Terrenos" bar (in its own brown tone) separate from the color-identity bars.
