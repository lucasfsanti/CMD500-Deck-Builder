## 1. Mana cost capture

- [x] 1.1 Add a `extractManaCost` helper (new `extension/src/lib/capture/mana-cost.ts`, mirroring `page-image.ts`'s `extractPageImageUrl`) that reads a card row's `.deck-cmc .txt-mana abbr.mtg-symbol` elements and decodes each one's class-name slug via a lookup table (Portuguese number words for generic costs `um`→`1` through at least `vinte`→`20`, single letters for W/U/B/R/G/X/Y/Z, and the Phyrexian compound `bp`→black-Phyrexian), returning an ordered `string[]` of canonical symbol codes. If any pip's slug isn't in the table, the helper returns `undefined` for the whole card rather than a partial list. Verify with unit tests covering: a multi-symbol cost decodes to the right ordered codes, a card with no `.deck-cmc` returns `undefined`, and a card with one unrecognized slug returns `undefined` for the whole cost (not a partial array).
- [x] 1.2 Add `pageManaCostSymbols: string[] | undefined` to `CapturedCard` in `extension/src/lib/deck/types.ts` (flows through to `DeckCard` automatically). Verify the project typechecks (`npm run typecheck` or equivalent build step) with the new field in place.
- [x] 1.3 Wire `extractManaCost` into `parseDeckPage` (`deck-page-parser.ts`) so every captured card carries `pageManaCostSymbols`. Extend `test/fixtures/ligamagic-deck-full.html` with real `.deck-cmc` markup on at least one existing card (a plain multi-symbol cost) and confirm an existing land entry has none. Verify via `deck-page-parser.test.ts`: the costed card's `pageManaCostSymbols` matches the fixture's symbols in order, and the land's is `undefined`.

## 2. Mana cost icons in List view

- [x] 2.1 Add a small presentational component (e.g. `ManaCostIcons.tsx`) that renders one `<img>` per symbol code in `card.pageManaCostSymbols`, each hotlinked to `https://www.ligamagic.com.br/arquivos/img/mtg/symb/<code>.svg`, and renders nothing when the card has no captured mana cost. Verify with a component test: a card with symbols renders one image per symbol in order with the expected `src`, and a card with none renders no icons.
- [x] 2.2 Wire `ManaCostIcons` into `CardRow`, positioned between the card name and price. Verify via `CardRow.test.tsx`: a card with a captured mana cost shows its icons in that position; a card without shows none and the row lays out normally; existing drag, quantity-edit, removal, and illegal/over-budget marker tests still pass unchanged.
- [x] 2.3 Add pip sizing/spacing CSS in `panel.css` for the new icons within a List row. Verify visually via the `run` skill (task 4.2 covers the full manual pass; this can be checked incrementally while styling).

## 3. Per-zone name filter (Main Deck and Maybeboard)

- [x] 3.1 Add a `filterable?: boolean` prop to `ZoneSection`, plus local filter-text state (`useState`, not lifted to `TabRoot`). When `filterable` is set and the user has typed filter text, filter the zone's `cards` by case-insensitive substring match on `name` *before* calling `groupAndSortZone`, so groups left with zero matches disappear via `groupAndSortZone`'s existing empty-bucket skipping — no separate empty-group logic needed. Verify via `ZoneSection.test.tsx`: typing a substring narrows visible cards to matches only, a group with no matches after filtering doesn't render its label, and clearing the filter text restores every card.
- [x] 3.2 Render a filter input in the zone header only when `filterable` is set, and make the zone's header count reflect the filtered card set. Verify via `ZoneSection.test.tsx`: a non-`filterable` zone (e.g. hero) renders no filter input, a `filterable` zone does, and the header count changes as the filter narrows the visible cards.
- [x] 3.3 Set `filterable` on the Main Deck and Maybeboard `ZoneSection` call sites in `TabRoot.tsx` only (not Comandante, Comandante Parceiro, or the Maybeboard-as-hero-column instance's siblings — confirm Maybeboard is the only non-hero zone besides Main Deck). Verify by rendering `TabRoot` (or the relevant zones) and confirming exactly those two zones expose a filter input.
- [x] 3.4 Confirm filtering never changes deck-wide totals: with a zone's filter narrowing its visible cards, the budget total, card count, and legality summary (all computed in `TabRoot` from the full, unfiltered `cards` array) stay unchanged. Verify with a test that types filter text into one zone and asserts `BudgetGauge`/`CardCountGauge`/`LegalitySummary` output is unaffected.
- [x] 3.5 Add filter input styling in `panel.css` consistent with the existing zone-header look. Verify visually via the `run` skill (task 4.2).

## 4. End-to-end verification

- [x] 4.1 Run the full test suite and typecheck; verify everything passes, including all new and updated tests from tasks 1-3.
- [x] 4.2 Launch the extension via the `run` skill against a real captured LigaMagic deck (or the existing manual test flow) and confirm in the browser: mana-cost icons render correctly for costed cards and are absent for lands in List view; typing in Main Deck's filter narrows only Main Deck, typing in Maybeboard's filter narrows only Maybeboard; budget, card count, and legality panels stay correct while a filter is active; existing drag-and-drop, quantity editing, and removal still work.
