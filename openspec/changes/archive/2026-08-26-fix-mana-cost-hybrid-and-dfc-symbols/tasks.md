## 1. Hybrid mana symbol support

- [x] 1.1 Add all ten two-color hybrid slugs to `SLUG_TO_SYMBOL` in `extension/src/lib/capture/mana-cost.ts` (`bg`, `gu` confirmed live; `bu`, `bw`, `br`, `gr`, `gw`, `ru`, `rw`, `uw` added by the same alphabetically-ordered convention, colors sorted `b < g < r < u < w`), mapping each to a slash-free `"<Letter><Letter>"` canonical code matching the real asset filename (e.g. `bg` → `"BG"`, confirmed live — a `/` 404s). Verify with unit tests: `bg` and `gu` decode correctly in a multi-symbol cost, and at least one of the inferred pairs decodes too.
- [x] 1.2 Add regression fixtures/tests mirroring the real bug reports' `.deck-cmc` markup: `Lluwen, Imperfect Naturalist` (`{B/G}{B/G}`, two `mtg-symbol-bg` abbrs) and `Revitalizing Repast` (`{B/G}`, one `mtg-symbol-bg` abbr) both now decode to a defined cost instead of `undefined`. Verify via `mana-cost.test.ts`.

## 2. Suppress mana-cost icons for split cards only (transform/modal_dfc/adventure/meld verified unaffected or left as a known limitation)

- [x] 2.1 In `CardRow.tsx`, gate `<ManaCostIcons>` rendering so it renders nothing when `card.enrichment?.layout` is `"split"` — even when `card.pageManaCostSymbols` is defined. A card with any other layout (including `transform`, `modal_dfc`, `adventure`, `meld`) or with enrichment not yet resolved keeps showing its page-captured cost unchanged. Verify via `CardRow.test.tsx`: a card with `layout: "split"` and a defined `pageManaCostSymbols` shows no icons; cards with `transform`/`modal_dfc`/`adventure` layouts still show their icons (no regression); a card with `enrichmentStatus: "pending"` still shows its page-captured cost.
- [x] 2.2 Before finalizing the suppressed-layout set, verify each candidate layout against real `.deck-cmc` markup (and Scryfall's API for ground truth) rather than assuming from the layout name alone. Resolved: `meld` (Bruna, the Fading Light) and `transform` (Delver of Secrets) never concatenate — excluded. `split` (Fire // Ice; Roaring Furnace // Steaming Sauna, confirmed `layout: "split"` via Scryfall) concatenates consistently — included. `adventure` (Thranduil, Sindarin Liege // Silvan Rally; Brazen Borrower // Petty Theft; Bonecrusher Giant // Stomp) concatenates for some real cards and not others under the same layout value with no detectable distinguishing signal — left unsuppressed as a documented limitation rather than an unreliable rule. `modal_dfc` was never directly confirmed either way; given `adventure` already proved same-layout inconsistency is real, it's treated the same as `adventure` (unsuppressed) rather than trusted from a single unverified example.
- [x] 2.3 Confirm `card.enrichment?.layout` for a captured card whose enrichment hasn't resolved yet (`enrichmentStatus: "pending"`) is `undefined`, not a stale or default value, so pending cards fall through to "show the page-captured cost" rather than being accidentally suppressed. Verify via existing type/enrichment tests or a targeted `CardRow.test.tsx` case if not already covered.

## 3. Verification

- [x] 3.1 Run the full test suite and typecheck; verify everything passes, including all new and updated tests from tasks 1-2.
- [x] 3.2 Launch the extension via a real-browser pass against `https://www.ligamagic.com.br/?view=dks/deck&id=10171831` and confirm: `Lluwen, Imperfect Naturalist` and `Revitalizing Repast` now show their `{B/G}` mana-cost icons, resolving to real, non-404ing images; ordinary single-faced cards elsewhere in the deck are unaffected. `Thranduil, Sindarin Liege // Silvan Rally` is expected to still show its (concatenated, incorrect) page-captured cost — that's the documented `adventure` limitation from task 2.2, not a regression to fix here.
