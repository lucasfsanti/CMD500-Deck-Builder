## 1. Remove the Sideboard zone

- [x] 1.1 Remove `"sideboard"` from `ZONES`/`Zone` in `extension/src/lib/deck/types.ts` and let the compiler surface every now-incomplete `Record<Zone, ...>` site; verify `npm run typecheck` fails at each site before the following tasks fix them.
- [x] 1.2 In `extension/src/lib/capture/zone-labels.ts`, change the `"Sideboard"` header mapping to resolve to `"maybeboard"`; update `zone-labels`'s existing tests (if any) and add a case asserting `zoneForHeaderLabel("Sideboard") === "maybeboard"`.
- [x] 1.3 Update `extension/src/lib/capture/deck-page-parser.ts`'s fallback-zone comment/logic if needed now that `"sideboard"` can never be returned by `zoneForHeaderLabel`, and update `deck-page-parser.test.ts` fixtures/assertions that reference a distinct Sideboard zone to expect Maybeboard instead; verify `npm test -- deck-page-parser` passes.
- [x] 1.4 Remove the `sideboard` entry from `ZONE_LABELS` in `extension/src/ui/components/ZoneSection.tsx`; verify the component compiles with the narrowed `Zone` type.
- [x] 1.5 Update `extension/src/lib/export/generate-decklist.ts` to drop the separate Sideboard block from both export formats (folding those cards into the Maybeboard block via the zone change already made), and update `generate-decklist.test.ts` accordingly; verify `npm test -- generate-decklist` passes.
- [x] 1.6 Update `extension/src/lib/budget/calculate-budget.ts`'s comment (no logic change — `sideboard` was already excluded) and `extension/src/lib/analytics/bucket-counts.ts` if it references `sideboard` explicitly; verify `npm test -- calculate-budget bucket-counts` passes.
- [x] 1.7 Update `deck-state.test.ts`, `resolve-drop.test.ts`, and any other test with Sideboard fixtures/assertions across `extension/src`; verify `npm test` passes with zero remaining references to a `"sideboard"` zone value.

## 2. Drag visual feedback and pointer-accurate drop targeting

- [x] 2.1 In `extension/src/tab/TabRoot.tsx`, add `<DragOverlay>` inside `<DndContext>`, rendering a clone of the active card (`CardRow` for List mode, `CardVisualTile` for Visual mode) based on the drag's `active` data; verify a card visibly follows the cursor while dragging in a manual browser check.
- [x] 2.2 Set `collisionDetection={pointerWithin}` (from `@dnd-kit/core`) on the same `<DndContext>`, replacing the default `rectIntersection`; verify `resolve-drop.test.ts` still passes (it operates on the resolved `event.over.id`, independent of the collision strategy) and manually verify a Visual-mode tile grabbed near its bottom edge now drops into the zone under the cursor.
- [x] 2.3 While a card is dragging, suppress its normal in-place rendering (or keep it dimmed via the existing `c500-card--dragging`/`c500-tile--dragging` opacity) so the original and the overlay clone are not both fully visible at once; verify visually in both List and Visual view.
- [x] 2.4 Add/update `panel.css` styling for the `DragOverlay` clone (e.g. slight scale or shadow to read as "lifted") consistent with the existing `--dragging` opacity treatment; verify visually.

## 3. 99-card limit warning

- [x] 3.1 Add `extension/src/lib/organizer/calculate-card-count.ts` exporting `CARD_COUNT_CAP = 99` and a function computing the summed quantity across Main Deck and Comandante Parceiro (no basic-land exclusion), matching `calculate-budget.ts`'s shape; add `calculate-card-count.test.ts` covering under-cap, at-cap, and over-cap cases plus a case confirming Comandante and Maybeboard don't contribute; verify `npm test -- calculate-card-count` passes.
- [x] 3.2 Add a small gauge component (e.g. `CardCountGauge.tsx`) mirroring `BudgetGauge.tsx`'s over-cap visual treatment, showing the current count and, when over, the exact overage; add a component test mirroring `BudgetGauge.test.tsx`; verify `npm test -- CardCountGauge` passes.
- [x] 3.3 Wire `CardCountGauge` into `TabRoot.tsx`'s sidebar next to `BudgetGauge`, recomputed on every render alongside `budget`/`legality`; verify manually that adding cards past 99 in Main Deck triggers the over-limit visual state immediately.

## 4. List-mode artwork hover preview

- [x] 4.1 Add hover state and pointer tracking (`onPointerEnter`/`onPointerMove`/`onPointerLeave`) to `CardRow.tsx`, reusing `CardVisualTile`'s artwork-resolution (`pageImageUrl ?? enrichment?.imageUrl`) and placeholder fallback for cards with no resolvable art.
- [x] 4.2 Render a `position: fixed` preview anchored near the tracked pointer coordinates when hovered, suppressed while `isDragging`; add `panel.css` styling for the preview (reusing tile-art sizing/placeholder classes where practical).
- [x] 4.3 Add a `CardRow.test.tsx` case (or extend the existing one) covering: preview appears on hover, disappears on pointer-leave, and shows the placeholder treatment when artwork can't be resolved; verify `npm test -- CardRow` passes.
- [x] 4.4 Manually verify in the browser that hovering a List-mode row shows the artwork preview without interfering with drag-start or the quantity input's own click/pointerdown handling.

## 5. Brazilian Portuguese localization

- [x] 5.1 Translate all string literals and label maps (`FORMAT_LABELS`, `GROUPING_AXIS_LABELS`, status/error messages, `aria-label`s) in `extension/src/tab/TabRoot.tsx`. (`FORMAT_LABELS` values — "Commander 500"/"Commander 500 Duel" — kept as-is: proper format names, not generic copy.)
- [x] 5.2 Translate remaining UI-facing strings in `extension/src/ui/components/*.tsx` (`ZoneSection`, `CardRow`, `CardVisualTile`, `BudgetGauge`, `CardCountGauge`, `LegalitySummary`, `ExportMenu`, `charts/BarChart`) and any `aria-label`/`title` attributes among them — card names (`card.name`) stay untranslated. Also translated the readable-export zone labels (`generate-decklist.ts`) and the drag/move error messages in `deck-state.ts`, found during the sweep. Per user decision: "Comandante"/"Comandante Parceiro"/"Maybeboard" kept exactly as LigaMagic's own page shows them; "Main Deck" (this app's own invented label) translated to "Deck Principal". Also added a presentation-only PT-BR label map (`TYPE_DISPLAY_LABELS`/`COLOR_GROUP_DISPLAY_LABELS` in `group-sort.ts`) for zone group headers and chart bucket labels, keeping the underlying English classification keys that match Scryfall's data untouched.
- [x] 5.3 Update component tests that assert specific English UI strings to assert their Portuguese equivalents instead; verify `npm test` passes across the `ui/components` suite.
- [x] 5.4 Translate `README.md` in full. (Its only content is the H1 project slug "CMD500-Deck-Builder" — a repo name/identifier, not prose — left as-is; no natural-language content to translate.)
- [x] 5.5 Translate `extension/README.md` in full, including the developer-facing Setup, Loading-the-extension, Testing, and Playwright verify-script sections.
- [x] 5.6 Grep the translated files for any remaining hardcoded English UI string (excluding code identifiers, comments, and card names) and fix any misses found. (Also caught and translated `public/manifest.json`'s `name`/`description`/`action.default_title` — shown in Chrome's toolbar tooltip and extensions page — and `public/tab.html`'s `<title>`/`lang` attribute, neither originally listed in this task but genuinely user-facing UI text.)

## 6. Final verification

- [x] 6.1 Run `npm run typecheck` and `npm test` from `extension/` and confirm both pass cleanly with zero references to a Sideboard zone or English UI strings remaining in test expectations. (`npm run build` also verified clean.)
- [x] 6.2 Manually exercise the full-tab view in a browser against a real or fixture-backed LigaMagic deck page: confirm four zones render, drag-and-drop shows the cursor-following ghost and drops accurately in both List and Visual view, the 99-card gauge reacts live, the List-mode hover preview works, and all UI text is in Portuguese with card names untouched. Verified live against the real LigaMagic site (deck id 10174508, via a temporary Playwright script mirroring the project's own `scripts/verify-*.mjs` convention, deleted after use): title/zones/toggles/gauges/export menu all render in Portuguese; exactly four zones (Comandante, Comandante Parceiro, Deck Principal, Maybeboard — no Sideboard); dragging Llanowar Elves shows the `.c500-drag-overlay` ghost mid-drag and drops it correctly into Comandante Parceiro via `pointerWithin`; Deck Principal's count live-updates 99→98 on the move; the List-mode hover preview appears on hover and disappears on leave. Screenshots confirmed visually, then deleted along with the script.

  **Follow-up (post-verify-report) live checks**, closing two gaps the verify report flagged as SUGGESTIONs:
  - *Visual-mode off-center grab*: dragging a tile grabbed at its bottom-edge price div (227px into a 237px-tall tile, confirmed via `document.elementFromPoint`) still shows the ghost and drops correctly into Comandante Parceiro — `pointerWithin` is grab-point-independent as designed. (First attempt read as a failure because the raw `mouse.move`/`down`/`up` sequence doesn't auto-scroll like Playwright locator actions do, and the tall Visual-mode grid had scrolled the non-sticky small-zones column off-screen; fixed by widening the test viewport, not by touching product code.)
  - *Sideboard→Maybeboard fold*: route-intercepted `https://www.ligamagic.com.br/...` to serve `test/fixtures/ligamagic-deck-full.html` verbatim, then confirmed live in the rendered tab view (not just the parser unit test) that Maybeboard shows both the fixture's Maybeboard-headed card and its Sideboard-headed card together.
