## 1. Scryfall per-face mana cost data

- [x] 1.1 Add `parseScryfallManaCost` in a new `extension/src/lib/scryfall/mana-cost.ts`, converting a Scryfall bracket-notation cost string (e.g. `"{2}{G/U}{G/U}"`) into the existing canonical symbol-code array (e.g. `["2", "GU", "GU"]`): strip braces, sort a two-color hybrid symbol's letters alphabetically before concatenating (`{W/B}` and `{B/W}` both → `"BW"`), leave Phyrexian (`{B/P}` → `"BP"`) and plain letters/numbers/X/Y/Z/C/S as-is. Verify with unit tests: a multi-symbol cost with a plain hybrid pair, a cost with a reversed-order hybrid pair (confirms sorting), a Phyrexian symbol, and Thranduil's real two face costs (`"{2}{G/U}{G/U}"`, `"{1}{G/U}{G/U}"`) each decode to the exact arrays already observed live.
- [x] 1.2 In `ScryfallClient.toEnrichment` (`extension/src/lib/scryfall/client.ts`), when a card response's `card_faces` has more than one face with a non-empty `mana_cost`, populate a new per-face cost list using `parseScryfallManaCost`; otherwise leave it unset. Verify via `client.test.ts`: a mocked multi-face response (both faces costed) produces the parsed per-face arrays; a mocked response with one costed face and one blank (`mana_cost: ""`) produces no per-face data; a normal single-faced response is unaffected.
- [x] 1.3 Add `faceManaCosts: string[][] | undefined` to `CardEnrichment` in `extension/src/lib/deck/types.ts`. Update every existing `CardEnrichment`-shaped test literal across the codebase to include it (mirrors the same mechanical fix `pageManaCostSymbols` needed). Verify the project typechecks with the new field in place.

## 2. Rendering per-face costs with a `//` divider

- [x] 2.1 Extend `ManaCostIcons` to accept an alternate `symbolGroups: string[][]` prop (in addition to its existing single `symbols` prop), rendering each group's icons in order with a `//` text divider between groups. Verify with a component test: two groups render with a divider between them and no divider before the first or after the last group; the existing single-`symbols` behavior is unchanged.
- [x] 2.2 In `CardRow.tsx`, render `card.enrichment.faceManaCosts` via `ManaCostIcons`' `symbolGroups` prop whenever it's present, falling back to the existing single-`symbols` rendering of `card.pageManaCostSymbols` otherwise (including while enrichment is still pending). Remove the `DOUBLE_FACED_LAYOUTS` set and its associated suppression logic entirely — no layout-name check remains. Verify via `CardRow.test.tsx`: a card with `faceManaCosts` set to two groups renders both groups separated by `//`; a card with only a single real cost (`faceManaCosts` unset) renders exactly as before, unaffected; a card with enrichment still pending shows its page-captured `pageManaCostSymbols` unchanged; a split-layout card (previously fully suppressed) now shows its two separated costs instead of nothing.

## 3. Verification

- [x] 3.1 Run the full test suite and typecheck; verify everything passes, including all new and updated tests from tasks 1-2.
- [x] 3.2 Launch the extension via a real-browser pass against `https://www.ligamagic.com.br/?view=dks/deck&id=10171831` and confirm: `Thranduil, Sindarin Liege // Silvan Rally` now shows `{2}{G/U}{G/U} // {1}{G/U}{G/U}` as two separated icon groups (not the old 6-symbol jumble, not suppressed); a split card (e.g. `Fire // Ice`, in the deck used for `fix-mana-cost-hybrid-and-dfc-symbols`'s own verification) now shows its two costs separated, rather than nothing; `Lluwen, Imperfect Naturalist` and other single-real-cost cards render unchanged.
